# A default java.util.logging configuration.
# (All App Engine logging is through java.util.logging by default).
#
# To use this configuration, copy it into your application's WEB-INF
# folder and add the following to your appengine-web.xml:
# 
# <system-properties>
#   <property name="java.util.logging.config.file" value="WEB-INF/logging.properties"/>
# </system-properties>
#

# Set the default logging level for all loggers to WARNING
.level = WARNING

# Set logging level for our classes
com.wadpam.level = ${logging.level}

# Set logging level for spring security
org.springframework.web.servlet.level = ALL

# Set logging level for spring security
org.springframework.security.level = ALL

#set logging level for jgoogletracker
com.dmurph.tracking.level = FINE

#set logging level for maodao
net.sf.mardao.api.level = ALL